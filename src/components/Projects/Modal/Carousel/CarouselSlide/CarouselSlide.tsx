import React, { FC } from 'react';

interface IProps {
    images: string[];
    active: boolean;
}

const CarouselSlide: FC<IProps> = props => {
    return <div />;
};

export default CarouselSlide;
